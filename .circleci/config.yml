version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.6.4
    steps:
      - checkout
      - run: echo "A first hello"
      # install ffmpeg
      - run: echo "deb http://ppa.launchpad.net/jonathonf/ffmpeg-4/ubuntu bionic main" >> /etc/apt/sources.list
      - run: apt install gnupg
      - run: apt-key adv --keyserver keyserver.ubuntu.com --recv-keys 4AB0F789CBA31744CC7DA76A8CF63AD3F06FC659
      - run: apt install ffmpeg && ffmpeg -version
